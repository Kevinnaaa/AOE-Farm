--[[
    Sailor Piece - ALL IN ONE
    AOE Farm + Island Dropdown + Anti AFK
    Close with TERMINATE button
--]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")

local Farming = false
local ScriptActive = true
local AntiAFKEnabled = true
local Range = 30
local SelectedIsland = ""

-- Island Data
local Islands = {
    ["Starter Island Sea 2"] = {name = "StarterIsland_Sea2", desc = "Begin your new journey"},
    ["Bizarre Island"] = {name = "BizarreIsland", desc = "Where reality bends and twists"},
    ["Punch Island"] = {name = "PunchIsland", desc = "Where fists decide everything"},
    ["World Island Sea 2"] = {name = "WorldIsland", desc = "The gateway between seas"}
}

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AllInOne"
ScreenGui.Parent = game:GetService("CoreGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 240, 0, 250)
Frame.Position = UDim2.new(0, 10, 0, 10)
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 25)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Text = "⚔️ All In One"
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 14
Title.Parent = Frame

-- Status
local Status = Instance.new("TextLabel")
Status.Size = UDim2.new(1, -10, 0, 20)
Status.Position = UDim2.new(0, 5, 0, 30)
Status.BackgroundTransparency = 1
Status.TextColor3 = Color3.fromRGB(0, 255, 0)
Status.Text = "Ready | AFK: ON | Range: " .. Range
Status.TextXAlignment = Enum.TextXAlignment.Left
Status.Font = Enum.Font.SourceSans
Status.TextSize = 11
Status.Parent = Frame

-- Dropdown Label
local DropLabel = Instance.new("TextLabel")
DropLabel.Size = UDim2.new(1, -10, 0, 18)
DropLabel.Position = UDim2.new(0, 5, 0, 52)
DropLabel.BackgroundTransparency = 1
DropLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
DropLabel.Text = "📍 Select Island:"
DropLabel.TextXAlignment = Enum.TextXAlignment.Left
DropLabel.Font = Enum.Font.SourceSans
DropLabel.TextSize = 11
DropLabel.Parent = Frame

-- Dropdown Button
local DropBtn = Instance.new("TextButton")
DropBtn.Size = UDim2.new(1, -10, 0, 28)
DropBtn.Position = UDim2.new(0, 5, 0, 70)
DropBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
DropBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
DropBtn.Text = "▼ Choose Island"
DropBtn.Font = Enum.Font.SourceSans
DropBtn.TextSize = 13
DropBtn.Parent = Frame

-- Dropdown List
local DropList = Instance.new("ScrollingFrame")
DropList.Size = UDim2.new(1, -10, 0, 0)
DropList.Position = UDim2.new(0, 5, 0, 100)
DropList.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
DropList.BorderSizePixel = 0
DropList.Visible = false
DropList.Parent = Frame

local listY = 0
for displayName, islandData in pairs(Islands) do
    local Option = Instance.new("TextButton")
    Option.Size = UDim2.new(1, 0, 0, 28)
    Option.Position = UDim2.new(0, 0, 0, listY)
    Option.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    Option.TextColor3 = Color3.fromRGB(255, 255, 255)
    Option.Text = displayName
    Option.Font = Enum.Font.SourceSans
    Option.TextSize = 12
    Option.Parent = DropList
    
    Option.MouseButton1Click:Connect(function()
        SelectedIsland = displayName
        DropBtn.Text = "📍 " .. displayName
        DropList.Visible = false
    end)
    
    listY = listY + 30
end
DropList.CanvasSize = UDim2.new(0, 0, 0, listY)

DropBtn.MouseButton1Click:Connect(function()
    DropList.Visible = not DropList.Visible
    if DropList.Visible then
        DropList.Size = UDim2.new(1, -10, 0, listY)
    else
        DropList.Size = UDim2.new(1, -10, 0, 0)
    end
end)

-- Travel Button
local TravelBtn = Instance.new("TextButton")
TravelBtn.Size = UDim2.new(1, -10, 0, 25)
TravelBtn.Position = UDim2.new(0, 5, 0, 103)
TravelBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 150)
TravelBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
TravelBtn.Text = "🏃 TRAVEL"
TravelBtn.Font = Enum.Font.SourceSansBold
TravelBtn.TextSize = 13
TravelBtn.Parent = Frame

TravelBtn.MouseButton1Click:Connect(function()
    if SelectedIsland == "" then
        Status.Text = "Select an island first!"
        return
    end
    
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
        local island = Workspace:FindFirstChild(Islands[SelectedIsland].name)
        if island then
            local targetPart = island:FindFirstChildWhichIsA("BasePart")
            if targetPart then
                Status.Text = "🏃 Traveling..."
                local oldSpeed = char.Humanoid.WalkSpeed
                char.Humanoid.WalkSpeed = 250
                char.Humanoid:MoveTo(targetPart.Position)
                task.delay(3, function()
                    if char and char:FindFirstChild("Humanoid") then
                        char.Humanoid.WalkSpeed = oldSpeed
                        Status.Text = "✅ Arrived!"
                    end
                end)
            end
        else
            Status.Text = "❌ Island not loaded!"
        end
    end
end)

-- Start/Stop Farm Button
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(1, -10, 0, 30)
ToggleBtn.Position = UDim2.new(0, 5, 0, 135)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Text = "▶ START FARMING"
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.TextSize = 14
ToggleBtn.Parent = Frame

-- Anti AFK Toggle Button
local AFKBtn = Instance.new("TextButton")
AFKBtn.Size = UDim2.new(1, -10, 0, 25)
AFKBtn.Position = UDim2.new(0, 5, 0, 170)
AFKBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
AFKBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AFKBtn.Text = "🔄 Anti AFK: ON"
AFKBtn.Font = Enum.Font.SourceSansBold
AFKBtn.TextSize = 12
AFKBtn.Parent = Frame

-- Terminate Button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(1, -10, 0, 25)
CloseBtn.Position = UDim2.new(0, 5, 0, 200)
CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 30, 30)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Text = "⏏ TERMINATE"
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 13
CloseBtn.Parent = Frame

-- Info
local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(1, -10, 0, 18)
InfoLabel.Position = UDim2.new(0, 5, 0, 228)
InfoLabel.BackgroundTransparency = 1
InfoLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
InfoLabel.Text = "Farm + Travel + Anti AFK"
InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
InfoLabel.Font = Enum.Font.SourceSans
InfoLabel.TextSize = 10
InfoLabel.Parent = Frame

-- Button Functions
ToggleBtn.MouseButton1Click:Connect(function()
    Farming = not Farming
    if Farming then
        ToggleBtn.Text = "⏹ STOP FARMING"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 30, 30)
    else
        ToggleBtn.Text = "▶ START FARMING"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
        Status.Text = "Stopped | AFK: " .. (AntiAFKEnabled and "ON" or "OFF")
    end
end)

AFKBtn.MouseButton1Click:Connect(function()
    AntiAFKEnabled = not AntiAFKEnabled
    if AntiAFKEnabled then
        AFKBtn.Text = "🔄 Anti AFK: ON"
        AFKBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    else
        AFKBtn.Text = "🔄 Anti AFK: OFF"
        AFKBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    end
    Status.Text = (Farming and "Farming" or "Ready") .. " | AFK: " .. (AntiAFKEnabled and "ON" or "OFF")
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScriptActive = false
    Farming = false
    ScreenGui:Destroy()
    print("All scripts terminated!")
end)

-- Get NPCs in range
local function getNPCsInRange()
    local char = LocalPlayer.Character
    if not char then return {} end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return {} end
    
    local nearby = {}
    local npcFolder = Workspace:FindFirstChild("NPCs")
    
    if npcFolder then
        for _, obj in pairs(npcFolder:GetChildren()) do
            if obj:IsA("Model") and obj:FindFirstChild("Humanoid") then
                if obj.Humanoid.Health > 0 then
                    local npcRoot = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Head")
                    if npcRoot then
                        local dist = (root.Position - npcRoot.Position).Magnitude
                        if dist <= Range then
                            table.insert(nearby, {root = npcRoot, dist = dist})
                        end
                    end
                end
            end
        end
    end
    
    table.sort(nearby, function(a, b) return a.dist < b.dist end)
    return nearby
end

-- AOE Farm loop
task.spawn(function()
    while ScriptActive do
        if Farming then
            pcall(function()
                local char = LocalPlayer.Character
                if not char then return end
                
                local npcs = getNPCsInRange()
                if #npcs > 0 then
                    Status.Text = "⚔️ " .. #npcs .. " enemies | AFK: " .. (AntiAFKEnabled and "ON" or "OFF")
                    for _, npc in pairs(npcs) do
                        local myRoot = char:FindFirstChild("HumanoidRootPart")
                        if myRoot then
                            myRoot.CFrame = CFrame.lookAt(myRoot.Position, Vector3.new(npc.root.Position.X, myRoot.Position.Y, npc.root.Position.Z))
                            VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
                            task.wait(0.03)
                            VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
                        end
                    end
                else
                    Status.Text = "No enemies | AFK: " .. (AntiAFKEnabled and "ON" or "OFF")
                end
            end)
        end
        task.wait(0.1)
    end
end)

-- Anti AFK loop
task.spawn(function()
    while ScriptActive do
        if AntiAFKEnabled then
            pcall(function()
                local camera = workspace.CurrentCamera
                if camera then
                    camera.CFrame = camera.CFrame * CFrame.Angles(0, 0.02, 0)
                end
                local char = LocalPlayer.Character
                if char and char:FindFirstChild("Humanoid") then
                    char.Humanoid:Move(Vector3.new(0.1, 0, 0), true)
                end
            end)
        end
        task.wait(30)
    end
end)

print("All In One Ready! Farm | Travel | Anti AFK")
