--[[
    Sailor Piece - AOE AUTO FARM + ISLAND TRAVEL
    Hits ALL enemies within range + Island speed-travel
    Matches your exact island names
    Close with TERMINATE button
--]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")

local Farming = false
local ScriptActive = true
local Range = 30 -- Studs

-- Island Data (matching YOUR game)
local Islands = {
    ["Starter Island Sea 2"] = {
        name = "StarterIsland_Sea2",
        desc = "Begin your new journey"
    },
    ["Bizarre Island"] = {
        name = "BizarreIsland",
        desc = "Where reality bends and twists"
    },
    ["Punch Island"] = {
        name = "PunchIsland",
        desc = "Where fists decide everything"
    },
    ["World Island Sea 2"] = {
        name = "WorldIsland",
        desc = "The gateway between seas"
    }
}

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AOEFarm"
ScreenGui.Parent = game:GetService("CoreGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 240, 0, 260)
Frame.Position = UDim2.new(0, 10, 0, 10)
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 25)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Text = "⚔️ AOE Farm + Travel"
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 14
Title.Parent = Frame

-- Status
local Status = Instance.new("TextLabel")
Status.Size = UDim2.new(1, -10, 0, 20)
Status.Position = UDim2.new(0, 5, 0, 30)
Status.BackgroundTransparency = 1
Status.TextColor3 = Color3.fromRGB(0, 255, 0)
Status.Text = "Status: Ready"
Status.TextXAlignment = Enum.TextXAlignment.Left
Status.Font = Enum.Font.SourceSans
Status.TextSize = 12
Status.Parent = Frame

-- Island Title
local IslandTitle = Instance.new("TextLabel")
IslandTitle.Size = UDim2.new(1, -10, 0, 20)
IslandTitle.Position = UDim2.new(0, 5, 0, 52)
IslandTitle.BackgroundTransparency = 1
IslandTitle.TextColor3 = Color3.fromRGB(255, 200, 0)
IslandTitle.Text = "📍 Select Island to Travel:"
IslandTitle.TextXAlignment = Enum.TextXAlignment.Left
IslandTitle.Font = Enum.Font.SourceSans
IslandTitle.TextSize = 11
IslandTitle.Parent = Frame

-- Island Buttons
local yPos = 72
for displayName, islandData in pairs(Islands) do
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -10, 0, 30)
    Btn.Position = UDim2.new(0, 5, 0, yPos)
    Btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.Text = displayName .. "\n" .. islandData.desc
    Btn.Font = Enum.Font.SourceSans
    Btn.TextSize = 11
    Btn.TextWrapped = true
    Btn.Parent = Frame
    
    Btn.MouseButton1Click:Connect(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
            local island = Workspace:FindFirstChild(islandData.name)
            if island then
                local targetPart = island:FindFirstChildWhichIsA("BasePart")
                if targetPart then
                    Status.Text = "🏃 Traveling to " .. displayName .. "..."
                    local oldSpeed = char.Humanoid.WalkSpeed
                    char.Humanoid.WalkSpeed = 250
                    char.Humanoid:MoveTo(targetPart.Position)
                    task.delay(3, function()
                        if char and char:FindFirstChild("Humanoid") then
                            char.Humanoid.WalkSpeed = oldSpeed
                            Status.Text = "✅ Arrived at " .. displayName
                        end
                    end)
                end
            else
                Status.Text = "❌ Island not loaded!"
            end
        end
    end)
    
    yPos = yPos + 33
end

-- Start/Stop Button
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(1, -10, 0, 35)
ToggleBtn.Position = UDim2.new(0, 5, 0, yPos + 5)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Text = "▶ START FARMING"
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.TextSize = 14
ToggleBtn.Parent = Frame

-- Info
local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(1, -10, 0, 20)
InfoLabel.Position = UDim2.new(0, 5, 0, yPos + 42)
InfoLabel.BackgroundTransparency = 1
InfoLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
InfoLabel.Text = "🎯 Range: " .. Range .. " studs | Safe speed travel"
InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
InfoLabel.Font = Enum.Font.SourceSans
InfoLabel.TextSize = 10
InfoLabel.Parent = Frame

-- Terminate Button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(1, -10, 0, 25)
CloseBtn.Position = UDim2.new(0, 5, 0, yPos + 64)
CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 30, 30)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Text = "⏏ TERMINATE"
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 13
CloseBtn.Parent = Frame

ToggleBtn.MouseButton1Click:Connect(function()
    Farming = not Farming
    if Farming then
        ToggleBtn.Text = "⏹ STOP FARMING"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 30, 30)
        Status.Text = "Status: Farming..."
    else
        ToggleBtn.Text = "▶ START FARMING"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
        Status.Text = "Status: Stopped"
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScriptActive = false
    Farming = false
    ScreenGui:Destroy()
    print("Script terminated!")
end)

-- Get all NPCs in range
local function getNPCsInRange()
    local char = LocalPlayer.Character
    if not char then return {} end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return {} end
    
    local nearby = {}
    local npcFolder = Workspace:FindFirstChild("NPCs")
    
    if npcFolder then
        for _, obj in pairs(npcFolder:GetChildren()) do
            if obj:IsA("Model") and obj:FindFirstChild("Humanoid") then
                local humanoid = obj.Humanoid
                if humanoid.Health > 0 then
                    local npcRoot = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Head")
                    if npcRoot then
                        local dist = (root.Position - npcRoot.Position).Magnitude
                        if dist <= Range then
                            table.insert(nearby, {model = obj, root = npcRoot, dist = dist})
                        end
                    end
                end
            end
        end
    end
    
    table.sort(nearby, function(a, b) return a.dist < b.dist end)
    return nearby
end

-- AOE Farming loop
task.spawn(function()
    while ScriptActive do
        if Farming then
            pcall(function()
                local char = LocalPlayer.Character
                if not char then
                    task.wait(0.5)
                    return
                end
                
                local npcs = getNPCsInRange()
                
                if #npcs > 0 then
                    Status.Text = "⚔️ Hitting " .. #npcs .. " enemies"
                    
                    for _, npc in pairs(npcs) do
                        local npcRoot = npc.root
                        local myRoot = char:FindFirstChild("HumanoidRootPart")
                        
                        if myRoot then
                            myRoot.CFrame = CFrame.lookAt(myRoot.Position, Vector3.new(npcRoot.Position.X, myRoot.Position.Y, npcRoot.Position.Z))
                            
                            VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
                            task.wait(0.03)
                            VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
                        end
                    end
                else
                    Status.Text = "No enemies in range"
                end
            end)
        end
        task.wait(0.1)
    end
end)

print("AOE Farm + Island Travel Ready!")
print("Islands: Starter Sea 2 | Bizarre | Punch | World Sea 2")
